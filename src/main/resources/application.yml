server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/graduation_project?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8&useSSL=false&useCursorFetch=true
    username: root
    password: 123456
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
     
mybatis:
  mapper-locations:   classpath:com/lrw/mapper/*.xml
  
  
logging:
  config: classpath:logback.xml